
<% @comments.each do |comment| %>

    <div class="comment_box">
        <b><%= comment.commenter %></b>
        <p><%= comment.body %> </p>
        <% if false %>
        TODO: Find some way to delete comments!!  
        <p>
        <%= link_to 'Destroy Comment', comment,
                :confirm => 'Are you sure?',
                :method => :delete %>
        <p>
        <% end %>
    </div>
<% end %>
